# https://lightning.ai/docs/pytorch/stable/api/lightning.pytorch.callbacks.LearningRateFinder.html#lightning.pytorch.callbacks.LearningRateFinder

lr_finder:
  _target_: lightning.pytorch.callbacks.LearningRateFinder
  min_lr: 0.00001
  max_lr: 0.01
  num_training_steps:  100
  mode: exponential
  update_attr: True